services:
  php:
    volumes:
      - type: bind
        source: '${HOME}/.config/composer/auth.json'
        target: '/tmp/database-home/.composer/auth.json'
        read_only: true
      - type: bind
        source: '${HOME}/.ssh'
        target: '/tmp/database-home/.ssh'
        read_only: true
